# HMS Frontend (Vite + React + Tailwind)
Quick start:
1. Install dependencies
   ```
   cd frontend
   npm install
   ```
2. Start dev server
   ```
   npm run dev
   ```
3. Build for production
   ```
   npm run build
   ```

Ensure backend is running (default at http://localhost:5000).
